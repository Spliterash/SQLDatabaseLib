rootProject.name = 'sql-database'

include(
        "jdbc",
        "base",
        "jdbc:types:sqlite:simple",
        "jdbc:types:sqlite:in-memory",
        "jdbc:types:sqlite:hikaricp",

        "jdbc:types:mysql:simple",
        "jdbc:types:mysql:hikaricp"
)
